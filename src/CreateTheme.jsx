import { createTheme, ThemeProvider } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: '"Helvetica Neue"', // Replace 'YourChosenFont' with the desired font
  },
});

export default theme;
